# Self-host / Docker Compose defaults.
# Browser-facing API path baked into the Vite build.
# Keep same-origin to avoid browser CORS.
VITE_AUTH_API_BASE_URL=/accounts-api

# Workshop origins allowed for /login?next=...
VITE_WORKSHOP_URL=https://workshop.aottg2.com

# Show LOGIN/ACCOUNT in navbar. Routes remain accessible when false.
SHOW_LOGIN_NAV=true

# Host port for docker compose.
WEB_PORT=8080

# Runtime nginx upstream for /accounts-api/*.
# No trailing slash. App calls /accounts-api/v1/*, proxy forwards to ${ACCOUNTS_API_UPSTREAM}/v1/*.
ACCOUNTS_API_UPSTREAM=https://aottg2.com

# Local npm dev auth service:
# ACCOUNTS_API_UPSTREAM=http://localhost:5010

# Local Docker auth service from Docker Desktop example:
# ACCOUNTS_API_UPSTREAM=http://host.docker.internal:5010
